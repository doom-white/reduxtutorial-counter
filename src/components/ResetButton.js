import React from "react";

const ResetButton = () => {
  return (
    <div className="reset-container">
      <button className="btn btn-danger btn-reset">R</button>
    </div>
  );
};

export default ResetButton;
